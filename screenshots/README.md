# Screenshots

These screenshots were captured from the local running version of **LLM_OUTPUT_VALIDATOR**.

## Included screenshots

1. `validator-console.png` — Main validator page showing schema selection, model field, output strategy, prompt template, variables JSON, safe mode, partial recovery, and schema difficulty.

2. `validation-result.png` — Successful validated output showing status, attempts, latency, correction status, and final structured JSON.

3. `attempt-history.png` — Attempt history showing the failed first attempt and corrected second attempt.

4. `schema-registry.png` — Schema registry page showing reusable schemas and difficulty scores.

5. `insights-dashboard.png` — Insights dashboard showing strategy performance and prompt reliability.

6. `failure-analysis.png` — Failure analysis page showing logged validation failures and raw response preview.

These screenshots represent the actual local project run and are included to make the repository easier to review.
